<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uytai.farmersp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".mvp.splash.SplashActivity" />
        <activity android:name=".mvp.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.register.RegisterActivity" />
        <activity android:name=".mvp.trader.SubscribeActivity" />
        <activity android:name=".mvp.feed.DetailActivity" />
        <activity android:name=".mvp.dangtin.DangTinActivity" />
        <activity android:name=".mvp.tindadang.TinDaDangActivity"></activity>
        <activity android:name=".mvp.tindadang.DetailTDDActivity"></activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>